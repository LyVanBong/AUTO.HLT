<UserControl
    x:Class="AUTO.HLT.ADMIN.Views.CheckWork.CheckWorkView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:AUTO.HLT.ADMIN.Views.CheckWork"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Label
            Grid.Row="0"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Content="Lịch sử chạy auto like comment ảnh đại diện" />
        <TextBox
            Grid.Row="1"
            Grid.Column="0"
            Margin="5"
            Text="{Binding SearchHistory}" />
        <Button
            Grid.Row="1"
            Grid.Column="1"
            Margin="5"
            Command="{Binding SearchHistoryCommand}"
            Content="Tìm kiếm" />
        <DataGrid
            Grid.Row="2"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            ItemsSource="{Binding DataHistoryAuto}" />
    </Grid>
</UserControl>